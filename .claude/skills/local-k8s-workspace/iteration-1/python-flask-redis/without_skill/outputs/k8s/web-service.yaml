apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: flask-redis
  labels:
    app: web
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: LoadBalancer
