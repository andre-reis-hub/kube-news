apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: kube-news
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP é suficiente: o banco só precisa ser acessível
  # internamente pela aplicação, não pelo browser.
  type: ClusterIP
