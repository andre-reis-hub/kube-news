# Estado local do Terraform (nunca commitar — contém dados sensíveis)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Variáveis com valores reais
terraform.tfvars

# Arquivos temporários
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
